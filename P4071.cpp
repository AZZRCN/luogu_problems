#include <bits/stdc++.h>
using namespace std;
vector<int>jc;//阶乘
vector<int> D;//错排
void yanhua(int limit){
    while(jc.size()<=limit)
}
int main(){
    jc.push_back(0);
    D.push_back(0);
    int T;
    cin >> T;
    for(int i = 1; i <= T; i++){

    }
}